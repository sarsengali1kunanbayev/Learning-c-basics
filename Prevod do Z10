#include <stdio.h>

int to_val(char c) {
    return c >= 'a' ? c - 'a' + 10 : c - '0';
}

int main() {
    char s[20];
    scanf("%s", s);
    int base = to_val(s[0]), res = 0;
    for (int i = 1; s[i]; i++)
        res = res * base + to_val(s[i]);
    printf("%d\n", res);
    return 0;
}
